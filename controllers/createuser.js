const User = require('../models/userSchema')

const userHandler = (req, res)=>{
  // newUser = new User();
  // newUser.save({
  //   name,
  //   nickname
  // }, (err, payload)=>{
  //   if(err){
  //     console.log(err);
  //   }
  // })

}
module.exports = {
  userHandler: userHandler
}